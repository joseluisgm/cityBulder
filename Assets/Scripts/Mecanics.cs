using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mecanics : MonoBehaviour
{
    public int mecanicToGuapa;
    // Start is called before the first frame update
    void Start()
    {
        mecanicToGuapa = 100;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
